# Воронка промахов — результат Недели 1 (честно)

Дата: 10 июня 2026. Что сделано за Неделю 1 плана (разбор архитектуры) и
что показала воронка промахов. Все цифры воспроизводимы:
`python scripts/miss_funnel.py --days 14`.

---

## Что сделано (3 инструмента)

| Инструмент | Что делает | Зачем |
|---|---|---|
| **Версионирование кэша** (`core/cache_version.py`) | Каждая запись кэша помечена `sha256(промпт+эвристики)[:8]`; правка правила → старые записи переклассифицируются | Раньше эффект правок был не виден, пока не появятся свежие статьи. Теперь — на следующем прогоне |
| **Маппер стадий** (`core/reject_stage.py`) | Вердикт → стадия воронки + причина | Причина отсева уже логировалась; это превращает её в декомпозицию S3/S4 |
| **Воронка промахов** (`scripts/miss_funnel.py`) | Для каждой публикации редактора — на какой стадии она «умерла» | Заменяет общее «recall ~32-54%, в основном покрытие» точной картой |

Тесты: 586 проходят (+30 новых). Ничего не сломано.

---

## Главный результат: дыра recall — это ПОКРЫТИЕ, не классификация

Окно: публикации редактора за 14 дней (408 публикаций).

| Стадия | Кол-во | Доля | Что значит |
|---|---|---|---|
| **S1** | 90 | 22.1% | источника нет в нашем списке |
| **S2** | 276 | 67.6% | источник есть, но статью не собрали |
| **S3** | 4 | 1.0% | собрали, убила эвристика |
| **S4** | 6 | 1.5% | собрали, отклонил ИИ |
| **S0/S5** | 32 | 7.8% | приняли (дошло до таблицы / потерялось в кластеризации) |

> **Покрытие (S1+S2) = 89.7% промахов. Классификация (S3+S4) = 2.5%.**

### Это честная цифра, а не удобная

Проверка матчинга, встроенная в инструмент: из 366 несматченных
публикаций у **90% (331) вообще нет близкого кандидата** в собранном
(лучший балл < 70) — это реальные промахи покрытия, а не ошибки
сопоставления. Лишь ~25 (7%) — «почти-матчи» (балл 70-84), где S2 мог
чуть завыситься.

Вывод **совпадает с независимым ревью архитектуры**: главный рычаг recall
— источники и скоринг ссылок, а НЕ промпт-классификатор. Месяцы правок
промпта почти не двигали recall, потому что чинили не ту стадию (S3/S4 =
2.5%), пока 90% терялось раньше (S1/S2).

---

## Что делать — по убыванию отдачи

### 1. S1 — добавить источники (90 промахов, ~22%)

Топ-домены, которые редактор использует, а мы НЕ краулим:

- `carscoops.com` (9) — крупное англоязычное авто-СМИ. Лёгкое добавление.
- `kolesa.ru` (5) — упомянут в заметках листа, но не активный источник.
- `static.nhtsa.gov` (8) — документы NHTSA (отзывы/safety).
- `nbki.ru` (4), `thesupercarblog.com` (3) и др.

⚠ Оговорка: S1 считается по домену Outer Link редактора. Часто это
ПЕРВОИСТОЧНИК (moex.com, nhtsa.gov), который мы достали бы через
агрегатор. Поэтому S1 — верхняя граница «дыр по источникам».

### 2. S2 — чинить сбор на конкретных доменах (276 промахов, ~68%)

Источник в списке, но статью не собрали → проблема RSS/скоринга ссылок
именно на этих сайтах:

- `t.me` (16) — мы сидим только на 3 телеграм-каналах, редактор берёт из
  большего числа. Расширить список каналов.
- `tass.ru` (15), `iz.ru` (9), `ria.ru` — крупные новостные ленты;
  вероятно, авто-раздел не парсится или тонет в скоринге.
- `carnewschina.com` (14), `cnevpost.com` (11) — китайские EV; статьи
  есть на сайте, но не достаём.
- `autostat.ru` (11), `auto.ru` (8), `naavtotrasse.ru` (7).

Это самый большой кусок. Для каждого домена: проверить, есть ли рабочий
RSS, не режет ли скоринг ссылок реальные статьи, не зависит ли от
Playwright (который мы отключили watchdog'ом).

### 3. S3/S4 — почти ничего (10 промахов, 2.5%)

Эвристики и ИИ почти не убивают живое (4 not_article + 6 LLM). Это
**подтверждает**, что блэклист и промпт — НЕ узкое место recall сейчас.
(Precision — другая ось; там промпт/секции важны.)

---

## Что это значит для плана недель 2-4

- **Сдвиг приоритета.** Раньше план вёл к retrieval-классификатору
  (Недели 3-4) как к главному рычагу. Воронка показывает: для **recall**
  главный рычаг — ПОКРЫТИЕ (S1/S2), а не классификатор. Retrieval
  остаётся ценным для **precision** (секции, «не нужно»), но recall он
  почти не тронет.
- **Конкретная работа на неделю покрытия:** добавить S1-домены;
  пройтись по топ-S2-доменам и починить их сбор. Это измеримо: повторный
  прогон воронки покажет сдвиг S2→S0.
- **Портал опубликованного** остаётся блокером для дублей (precision),
  вне моего контроля — эскалировать.

---

## Честные ограничения инструмента

- Кросс-язычный матчинг заголовков несовершенен (бренд/модель-токены
  ловятся, чисто-русский civic — хуже). Поэтому матчер настроен на
  точность: 42 уверенных матча, 366 «не нашли». Гистограмма баллов в
  отчёте показывает, что это в основном реальные промахи.
- S0 vs S5 не разделены (нужна история пушей).
- S1 — верхняя граница (домен Outer Link ≠ где мы бы нашли).
